version: '3'

services:
  service_a:
    image: golang:1.24
    container_name: service_a
    restart: always
    working_dir: /app
    ports:
      - 8080:8080
    command: >
      sh -c "go mod download && go test ./... && go run main.go"
    volumes:
      - .:/app
